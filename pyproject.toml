[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "osmomodpy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["hypomodpy", "numpy", "wxPython"]

[tool.setuptools]
packages = ["OsmoModPy"]
include-package-data = true